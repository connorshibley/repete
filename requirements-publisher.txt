# Publisher service only — the agent image stays lean without these.
# Pinned 2026-07-29 (W4-1); see requirements.txt for the bump rule.
fastapi==0.140.0
uvicorn==0.51.0
httpx==0.28.1        # Resend calls + FastAPI TestClient
# stripe==...        # uncomment when real billing keys exist (lazy import).
#                    # Pin it to whatever `pip install stripe` resolves at that
#                    # point and add it to requirements.lock in the same commit.
