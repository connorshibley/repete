# Publisher service only — the agent image stays lean without these.
fastapi>=0.115
uvicorn>=0.30
httpx>=0.27          # Resend calls + FastAPI TestClient
# stripe>=10          # uncomment when real billing keys exist (lazy import)
